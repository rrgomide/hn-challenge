# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
# MongoDB connection strings for different environments
MONGODB_URI_DEV=mongodb://localhost:27017/hn_challenge
MONGODB_URI_PROD=mongodb://mongodb:27017/hn_challenge

# AI Service Configuration
# You need to set at least one of these API keys for text summarization to work
# Google AI (Gemini) - Recommended
GOOGLE_GENERATIVE_AI_API_KEY=your_google_ai_api_key_here

# OpenAI (GPT-4o-mini) - Alternative
OPENAI_API_KEY=your_openai_api_key_here

# Notes:
# - MONGODB_URI_DEV is used when NODE_ENV=development
# - MONGODB_URI_PROD is used when NODE_ENV=production
# - For local development with Docker, use MONGODB_URI_DEV=mongodb://localhost:27017/hn_challenge
# - For production with Docker Compose, use MONGODB_URI_PROD=mongodb://mongodb:27017/hn_challenge
# - The AI service will prioritize Google AI if both API keys are provided
# - At least one AI API key is required for the text summarization feature to work
